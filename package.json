{
  "name": "lab-test-app",
  "version": "1.0.0",
  "description": "Lab Test Result App",
  "scripts": {
    "build": "echo \"No build step required\"",
    "cap:sync": "cap sync",
    "cap:build": "cap build android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/cli": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  }
}
